/**
 * main - Entry point
 *
 * Description : prints the alphabet in lowercase, followed by a new line
 *
 * Return: Always 0 (success)
 */
#include<stdio.h>
int main (void)
{
	char n;
	for(n = 'a'; a <= 'z' && n != 'q' && n != 'e'; a++)
	{
		putchar(n);

	}
	putchar('\n');
	return (0);
}
