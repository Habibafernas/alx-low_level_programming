#include "main.h"
/**
 * _abs - check the code
 *
 * Description : getting the abs value of n
 *
 * @n variable : n variable is integer
 *
 * Return: abs value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

